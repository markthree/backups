{
    "tasks": {
        "start:watch": "deno run -A src/watch.ts",
        "start:sync": "deno run -A src/sync.ts",
        "start:static-sync": "deno run -A src/static-sync.ts",
        "compile:watch": "deno compile -A src/watch.ts",
        "compile:sync": "deno compile -A src/sync.ts",
        "compile:static-sync": "deno compile -A src/static-sync.ts"
    }
}